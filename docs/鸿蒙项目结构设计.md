# 鸿蒙项目结构设计

## 📋 设计原则

**核心原则**：保持代码结构一致，仅增加鸿蒙原生目录

- ✅ JavaScript代码（`src/`）完全共享，无需修改
- ✅ 仅增加 `harmonyos/` 目录存放鸿蒙原生代码
- ✅ 依赖和构建配置都在 `harmonyos/` 子目录中
- ✅ 通过平台检测（Platform.OS）区分平台

---

## 一、项目目录结构

### 1.1 当前项目结构

```
ImageClassifierApp/
├── android/                    # Android原生代码
│   ├── app/
│   │   ├── build.gradle
│   │   └── src/
│   ├── build.gradle
│   ├── settings.gradle
│   └── gradle.properties
├── src/                        # 共享的JavaScript代码
│   ├── adapters/
│   ├── services/
│   ├── screens/
│   └── components/
├── public/                     # 公共资源
│   └── models/                 # AI模型文件
├── package.json               # 项目依赖
├── react-native.config.js     # RN配置
└── metro.config.js            # Metro配置
```

### 1.2 增加鸿蒙支持后的结构

```
ImageClassifierApp/
├── android/                    # Android原生代码（保持不变）
│   ├── app/
│   ├── build.gradle
│   └── settings.gradle
├── harmonyos/                  # 🆕 鸿蒙原生代码
│   ├── entry/                  # 应用入口
│   │   ├── src/
│   │   │   └── main/
│   │   │       ├── ets/        # ArkTS代码
│   │   │       ├── resources/  # 资源文件
│   │   │       └── module.json5 # 模块配置
│   │   └── build-profile.json5
│   ├── react-native-oh-library/ # RN鸿蒙适配库
│   ├── build-profile.json5     # 构建配置
│   ├── oh-package.json5        # 鸿蒙依赖配置
│   └── README.md
├── src/                        # 共享的JavaScript代码（完全共享）
│   ├── adapters/
│   │   ├── WebAdapters.js      # 需要扩展支持harmonyos
│   │   └── WebAdapters.native.js
│   ├── services/               # 所有服务层代码共享
│   ├── screens/               # 所有UI代码共享
│   └── components/
├── public/                     # 公共资源（共享）
│   └── models/
├── package.json               # 项目依赖（可能需要添加harmonyos脚本）
├── react-native.config.js     # RN配置（可能需要扩展）
└── metro.config.js            # Metro配置（共享）
```

---

## 二、代码共享策略

### 2.1 完全共享的代码

以下目录和文件**完全共享**，无需修改：

✅ **src/services/** - 所有业务逻辑服务
- `ImageClassifierService.js`
- `GalleryScannerService.js`
- `ImageStorageService.js`
- `MediaStoreService.js`
- 等等...

✅ **src/screens/** - 所有UI组件
- `HomeScreen.mobile.js`
- `CategoryScreen.mobile.js`
- `ImagePreviewScreen.mobile.js`
- 等等...

✅ **src/components/** - 所有通用组件

✅ **public/** - 所有公共资源
- `models/` - AI模型文件
- `icons/` - 图标文件

### 2.2 需要扩展的代码

#### src/adapters/WebAdapters.js

需要添加鸿蒙平台检测和适配：

```javascript
import { Platform } from 'react-native';

// 扩展平台检测
export const Platform = {
  OS: Platform.OS, // 'android' | 'ios' | 'web' | 'harmonyos'
  isHarmonyOS: Platform.OS === 'harmonyos',
  isAndroid: Platform.OS === 'android',
  isIOS: Platform.OS === 'ios',
  isWeb: Platform.OS === 'web',
};

// 文件系统适配
export const RNFS = {
  readFile: async (filePath, encoding) => {
    if (Platform.OS === 'harmonyos') {
      // 使用鸿蒙文件系统API
      const { HarmonyOSFS } = require('react-native-oh-library/react-native-fs');
      return await HarmonyOSFS.readFile(filePath, encoding);
    } else if (Platform.OS === 'android') {
      // 使用Android文件系统API
      const RNFS = require('react-native-fs');
      return await RNFS.readFile(filePath, encoding);
    }
    // ... 其他平台
  },
  // ... 其他方法
};

// 图片选择器适配
export const launchImageLibrary = (options, callback) => {
  if (Platform.OS === 'harmonyos') {
    // 使用鸿蒙图片选择器
    const { HarmonyOSImagePicker } = require('react-native-oh-library/react-native-image-picker');
    return HarmonyOSImagePicker.launchImageLibrary(options, callback);
  } else if (Platform.OS === 'android') {
    // 使用Android图片选择器
    const ImagePicker = require('react-native-image-picker');
    return ImagePicker.launchImageLibrary(options, callback);
  }
  // ... 其他平台
};
```

#### src/services/MediaStoreService.js

需要添加鸿蒙平台支持：

```javascript
import { NativeModules, Platform } from 'react-native';

const { MediaStoreModule } = NativeModules;
// 鸿蒙平台使用不同的模块名
const { MediaLibraryModule } = Platform.OS === 'harmonyos' 
  ? require('react-native-oh-library/harmonyos-media-library') 
  : { MediaLibraryModule: null };

class MediaStoreService {
  constructor() {
    if (Platform.OS === 'android') {
      this.isAvailable = MediaStoreModule != null;
      this.module = MediaStoreModule;
    } else if (Platform.OS === 'harmonyos') {
      this.isAvailable = MediaLibraryModule != null;
      this.module = MediaLibraryModule;
    } else {
      this.isAvailable = false;
      this.module = null;
    }
  }
  
  // 统一接口，内部根据平台调用不同实现
  async getAllImages(options = {}) {
    if (Platform.OS === 'android') {
      return await this.module.getAllImages(options);
    } else if (Platform.OS === 'harmonyos') {
      return await this.module.queryImages(options);
    }
    throw new Error('Unsupported platform');
  }
}
```

---

## 三、鸿蒙目录详细结构

### 3.1 harmonyos/ 目录结构

```
harmonyos/
├── entry/                      # 应用入口模块
│   ├── src/
│   │   └── main/
│   │       ├── ets/            # ArkTS代码
│   │       │   ├── entryability/
│   │       │   │   └── EntryAbility.ets  # 应用入口
│   │       │   ├── pages/
│   │       │   │   └── Index.ets         # 主页面（RN容器）
│   │       │   └── react-native/         # RN桥接代码
│   │       │       ├── ReactNativeHost.ets
│   │       │       └── modules/          # 原生模块
│   │       │           ├── MediaLibraryModule.ets
│   │       │           ├── FileSystemModule.ets
│   │       │           └── ImagePickerModule.ets
│   │       ├── resources/      # 资源文件
│   │       │   ├── base/
│   │       │   │   ├── element/
│   │       │   │   ├── media/
│   │       │   │   └── profile/
│   │       │   └── rawfile/    # 原始文件（如ONNX模型，如果需要）
│   │       └── module.json5    # 模块配置
│   └── build-profile.json5
├── react-native-oh-library/   # RN鸿蒙适配库（通过ohpm安装）
├── build-profile.json5         # 构建配置
├── oh-package.json5            # 鸿蒙依赖配置
└── README.md                   # 鸿蒙项目说明
```

### 3.2 关键文件说明

#### oh-package.json5

```json5
{
  "name": "xintualbum-harmonyos",
  "version": "1.0.0",
  "description": "芯图相册-鸿蒙版本",
  "main": "index.js",
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@react-native-oh-library/react-native-fs": "^1.0.0",
    "@react-native-oh-library/react-native-image-picker": "^1.0.0",
    "@react-native-oh-library/react-native-sqlite-storage": "^1.0.0",
    // ... 其他支持的库
  },
  "devDependencies": {
    "@ohos/hypium": "^1.0.0"
  }
}
```

#### entry/src/main/module.json5

```json5
{
  "module": {
    "name": "entry",
    "type": "entry",
    "description": "芯图相册主模块",
    "mainElement": "EntryAbility",
    "deviceTypes": [
      "phone",
      "tablet"
    ],
    "deliveryWithInstall": true,
    "installationFree": false,
    "pages": [
      "pages/Index"
    ],
    "abilities": [
      {
        "name": "EntryAbility",
        "srcEntry": "./ets/entryability/EntryAbility.ets",
        "description": "应用入口",
        "icon": "$media:icon",
        "label": "$string:entry_MainAbility",
        "startWindowIcon": "$media:icon",
        "startWindowBackground": "$color:start_window_background",
        "exported": true,
        "skills": [
          {
            "entities": [
              "entity.system.home"
            ],
            "actions": [
              "action.system.home"
            ]
          }
        ]
      }
    ],
    "requestPermissions": [
      {
        "name": "ohos.permission.READ_MEDIA",
        "reason": "需要读取相册图片",
        "usedScene": {
          "abilities": [
            "EntryAbility"
          ],
          "when": "inuse"
        }
      },
      {
        "name": "ohos.permission.WRITE_MEDIA",
        "reason": "需要保存图片",
        "usedScene": {
          "abilities": [
            "EntryAbility"
          ],
          "when": "inuse"
        }
      }
    ]
  }
}
```

---

## 四、构建配置

### 4.1 package.json 脚本扩展

```json
{
  "scripts": {
    "prebuild": "node scripts/generate-build-info.js",
    "android": "npm run prebuild && react-native run-android",
    "ios": "npm run prebuild && react-native run-ios",
    "harmonyos": "npm run prebuild && cd harmonyos && ohpm install && hvigorw assembleHap",
    "harmonyos:dev": "npm run prebuild && cd harmonyos && ohpm install && hvigorw assembleHap --mode debug",
    "harmonyos:release": "npm run prebuild && cd harmonyos && ohpm install && hvigorw assembleHap --mode release",
    "start": "react-native start",
    "test": "jest",
    "lint": "eslint ."
  }
}
```

### 4.2 react-native.config.js 扩展

```javascript
module.exports = {
  project: {
    android: {
      packageName: 'com.imageclassifier.v2'
    },
    harmonyos: {
      // 鸿蒙项目配置
      packageName: 'com.imageclassifier.v2',
      projectPath: './harmonyos'
    }
  },
  dependencies: {
    'react-native-sqlite-storage': {
      platforms: {
        android: {
          sourceDir: '../node_modules/react-native-sqlite-storage/platforms/android',
          packageImportPath: 'import org.pgsqlite.SQLitePluginPackage;',
          packageInstance: 'new SQLitePluginPackage()'
        },
        harmonyos: {
          // 鸿蒙平台配置
          sourceDir: '../node_modules/react-native-sqlite-storage/platforms/harmonyos'
        }
      }
    }
  }
};
```

---

## 五、平台检测和适配

### 5.1 平台检测

在代码中使用 `Platform.OS` 检测平台：

```javascript
import { Platform } from 'react-native';

if (Platform.OS === 'harmonyos') {
  // 鸿蒙平台特定代码
} else if (Platform.OS === 'android') {
  // Android平台特定代码
} else if (Platform.OS === 'ios') {
  // iOS平台特定代码
} else if (Platform.OS === 'web') {
  // Web平台特定代码
}
```

### 5.2 适配层设计

在 `src/adapters/WebAdapters.js` 中统一处理平台差异：

```javascript
// 统一的文件系统接口
export const RNFS = {
  readFile: async (filePath, encoding) => {
    switch (Platform.OS) {
      case 'harmonyos':
        return await harmonyosFS.readFile(filePath, encoding);
      case 'android':
        return await androidFS.readFile(filePath, encoding);
      case 'ios':
        return await iosFS.readFile(filePath, encoding);
      default:
        throw new Error(`Unsupported platform: ${Platform.OS}`);
    }
  },
  // ... 其他方法
};
```

---

## 六、开发工作流

### 6.1 开发流程

1. **修改共享代码**（`src/`）
   - 修改业务逻辑、UI组件等
   - 所有平台共享

2. **修改平台特定代码**
   - Android: `android/` 目录
   - 鸿蒙: `harmonyos/` 目录

3. **运行和测试**
   ```bash
   # Android
   npm run android
   
   # 鸿蒙
   npm run harmonyos:dev
   ```

### 6.2 构建流程

```bash
# 1. 安装依赖
npm install

# 2. 启动Metro服务器（共享）
npm start

# 3. 构建Android版本
npm run android

# 4. 构建鸿蒙版本
npm run harmonyos:release
```

---

## 七、优势总结

### 7.1 代码复用

- ✅ **JavaScript代码100%共享**
- ✅ **业务逻辑完全复用**
- ✅ **UI组件完全复用**
- ✅ **服务层代码完全复用**

### 7.2 维护成本

- ✅ **单一代码库**：所有平台代码在一个仓库
- ✅ **统一维护**：修改业务逻辑只需改一处
- ✅ **平台隔离**：原生代码各自独立，互不影响

### 7.3 开发效率

- ✅ **快速迭代**：修改共享代码，所有平台同步更新
- ✅ **统一测试**：业务逻辑测试一次，所有平台受益
- ✅ **代码一致性**：所有平台功能保持一致

---

## 八、注意事项

### 8.1 平台差异处理

- 使用适配层（`WebAdapters.js`）统一处理平台差异
- 避免在业务代码中直接使用平台特定API
- 通过 `Platform.OS` 检测平台，使用适配后的接口

### 8.2 原生模块

- Android原生模块在 `android/app/src/main/java/`
- 鸿蒙原生模块在 `harmonyos/entry/src/main/ets/react-native/modules/`
- 通过适配层统一暴露给JavaScript

### 8.3 资源文件

- 共享资源放在 `public/` 目录
- 平台特定资源放在各自平台目录
- 通过适配层统一访问路径

---

## 九、实施步骤

### 阶段1：创建鸿蒙目录结构（1天）

- [ ] 创建 `harmonyos/` 目录
- [ ] 初始化鸿蒙项目结构
- [ ] 配置 `oh-package.json5`
- [ ] 配置 `module.json5`

### 阶段2：集成React Native（2-3天）

- [ ] 安装 react-native-oh-library
- [ ] 配置RN桥接
- [ ] 测试RN基础功能

### 阶段3：适配原生模块（1-2周）

- [ ] 适配文件系统模块
- [ ] 适配图片选择器模块
- [ ] 适配数据库模块
- [ ] 适配其他原生模块

### 阶段4：扩展适配层（1周）

- [ ] 扩展 `WebAdapters.js` 支持鸿蒙
- [ ] 测试所有适配接口
- [ ] 确保功能一致性

### 阶段5：测试和优化（1周）

- [ ] 功能测试
- [ ] 性能测试
- [ ] 兼容性测试

---

**总结**：这种结构设计保持了代码的一致性，最大化代码复用，同时保持了各平台原生代码的独立性。非常适合多平台开发。

