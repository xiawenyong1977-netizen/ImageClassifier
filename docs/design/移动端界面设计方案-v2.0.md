# 芯图相册 - 移动端界面设计方案 v2.0

## 📋 文档信息

- **项目名称**: 芯图相册 (ImageClassifier App)
- **平台**: React Native (Android + iOS)
- **设计版本**: v2.0
- **创建日期**: 2025-01-20
- **最后更新**: 2025-01-20
- **设计目标**: 基于PC端功能，设计简洁高效的移动端界面

---

## 🎯 设计原则

### 核心原则
1. **功能对齐**: 与PC端保持功能一致
2. **移动优先**: 适配触摸操作和小屏幕
3. **简洁高效**: 减少层级，快速访问核心功能
4. **性能优先**: 懒加载、分页加载优化体验

### 技术约束
- Service层已完成移动端适配（SQLite、ONNX Runtime等）
- 复用PC端的Service层代码
- 使用React Native Navigation

---

## 📱 整体架构

### 导航结构

```
App Root
│
├─ Bottom Tab Navigator (底部导航)
│  ├─ 🏠 HomeScreen (首页)
│  ├─ 📦 StagingBoxScreen (暂存箱) ← 实际是CategoryScreen
│  └─ ⚙️ SettingsScreen (设置)
│
└─ Stack Navigator (堆栈导航)
   ├─ CategoryScreen (分类详情)
   └─ ImagePreviewScreen (图片预览)
```

### 页面清单

| 序号 | 页面名称 | 类型 | 主要功能 | 说明 |
|------|---------|------|---------|------|
| 1 | HomeScreen | Tab | 统计概览、分类入口、最近照片 | 首页 |
| 2 | StagingBoxScreen | Tab | 暂存箱管理 | 复用CategoryScreen |
| 3 | SettingsScreen | Tab | 应用设置 | 设置页 |
| 4 | CategoryScreen | Stack | 通用列表页（分类/城市/相似组） | 核心通用页 |
| 5 | ImagePreviewScreen | Stack | 图片预览和操作 | 详情页 |

**总计**: 5个页面（3个Tab + 2个Stack）

---

## 🏠 HomeScreen（首页）详细设计

### 功能概述
首页是用户的主要入口，提供快速访问各种分类和查看最近照片的功能。

### 布局结构

```
┌────────────────────────────────┐
│ 📱 芯图相册              🔔     │ ← 顶部导航栏
├────────────────────────────────┤
│                                │
│ ↓ ScrollView 向下滚动           │
│                                │
│ 📊 统计概览                    │
│ ┌──────┐ ┌──────┐             │
│ │📸总数 │ │📦暂存│             │
│ │ 256  │ │  12  │ ← 红点提醒   │
│ └──────┘ └──────┘             │
│ ┌──────┐ ┌──────┐             │
│ │✨今日 │ │🔍相似│             │
│ │  5   │ │  8   │             │
│ └──────┘ └──────┘             │
├────────────────────────────────┤
│ 📁 按内容分类          全部 >  │
│ ← 横向滚动 →                   │
│ ┌─────┐ ┌─────┐ ┌─────┐       │
│ │🖼️截图│ │📝证件│ │📄文档│       │
│ │ 45  │ │ 23  │ │ 18  │       │
│ │[图] │ │[图] │ │[图] │       │
│ └─────┘ └─────┘ └─────┘       │
├────────────────────────────────┤
│ 🔍 相似图片分组        全部 >  │
│ ┌───────────────────────┐     │
│ │ 组1 (5张相似) 95%    >│     │
│ │ [图][图][图] +2       │     │
│ └───────────────────────┘     │
│ ┌───────────────────────┐     │
│ │ 组2 (3张相似) 92%    >│     │
│ │ [图][图][图]          │     │
│ └───────────────────────┘     │
│ ... 最多显示8组                │
├────────────────────────────────┤
│ 🏙️ 按城市分类          全部 >  │
│ > 北京 (128)                  │
│ > 上海 (56)                   │
│ > 广州 (34)                   │
│ > 深圳 (28)                   │
│ ... 显示所有城市               │
├────────────────────────────────┤
│ 📸 最近照片            全部 >  │
│ ┌───┐┌───┐┌───┐             │
│ │   ││   ││   │             │
│ └───┘└───┘└───┘             │
│ ┌───┐┌───┐┌───┐             │
│ │   ││   ││   │ ← 3x4网格    │
│ └───┘└───┘└───┘             │
│ ... 显示12张                  │
├────────────────────────────────┤
│  [🏠首页] [📦暂存箱] [⚙️设置]  │ ← 底部Tab导航
└────────────────────────────────┘
              ↑
            FAB 🔄  扫描按钮（右下角浮动）
```

### 1. 统计概览区

#### 布局
- 2x2网格，4个统计卡片
- 卡片尺寸：约占屏幕宽度的45%（考虑间距）
- 卡片高度：80-100px

#### 卡片详细设计

**卡片1: 总图片数**
```javascript
{
  icon: "📸",
  title: "总图片",
  value: totalImagesCount,
  color: "#007AFF",
  onPress: null  // 不可点击
}
```

**卡片2: 暂存箱**
```javascript
{
  icon: "📦",
  title: "暂存箱",
  value: stagingBoxCount,
  color: "#FF9500",
  badge: stagingBoxCount > 0,  // 有图片时显示红点
  onPress: () => {
    // 切换到暂存箱Tab
    navigation.navigate('StagingBox')
  }
}
```

**卡片3: 今日新增**
```javascript
{
  icon: "✨",
  title: "今日新增",
  value: todayAddedCount,
  color: "#34C759",
  onPress: null
}
```

**卡片4: 相似组**
```javascript
{
  icon: "🔍",
  title: "相似组",
  value: similarityGroupsCount,
  color: "#AF52DE",
  onPress: () => {
    // 滚动到相似组区域
    scrollToSection('similarityGroups')
  }
}
```

#### 数据来源
```javascript
// 从 UnifiedDataService 获取
const totalImagesCount = await UnifiedDataService.getTotalImageCount();
const stagingBoxCount = await UnifiedDataService.getImageCountByCategory('tobecleaned');
const todayAddedCount = await UnifiedDataService.getTodayAddedCount();
const similarityGroupsCount = await UnifiedDataService.getSimilarityGroupsCount();
```

---

### 2. 按内容分类区

#### 布局
- 横向滚动（ScrollView horizontal）
- **显示规则**: 显示所有存在的分类（有多少显示多少）
- 卡片尺寸：100-120px宽 x 140-160px高
- 卡片间距：8-12px

#### 分类卡片组成
```
┌─────────┐
│  [图]   │ ← 代表图（80x80px，圆角8px）
│  🖼️截图  │ ← 分类名（12-14px）
│   45    │ ← 数量（16-18px，加粗）
└─────────┘
```

#### 数据结构
```javascript
const categories = [
  {
    id: 'screenshot',
    icon: '🖼️',
    name: '微信截图',
    count: 45,
    thumbnail: 'file://...'  // 该分类最新的1张图片
  },
  {
    id: 'document',
    icon: '📝',
    name: '证件照片',
    count: 23,
    thumbnail: 'file://...'
  },
  // ... 更多分类
];
```

#### 交互
```javascript
onPress: (category) => {
  navigation.navigate('Category', {
    category: category.id,
    fromScreen: 'Home'
  });
}
```

#### 分类列表
基于 `initialSettings.json` 中的 `categoryNameMap`:
- 微信截图 (screenshot)
- 证件照片 (document)
- 工作文档 (work_document)
- 生活照片 (life)
- 人物照片 (person)
- 风景照片 (landscape)
- 美食照片 (food)
- 会议记录 (meeting)
- 其他 (other)
- 待处置 (tobecleaned) - 仅在暂存箱显示

---

### 3. 相似图片分组区

#### 布局
- 垂直列表
- **显示规则**: 最多显示前8个相似组
- 按相似度降序排列
- 卡片高度：80-100px

#### 相似组卡片组成
```
┌──────────────────────────────┐
│ 🔍 组1 (5张相似)  95%      > │ ← 标题行
├──────────────────────────────┤
│ [图] [图] [图] +2            │ ← 缩略图（3张60x60px + 数量）
│                              │
│ 建议: 保留1张，删除4张         │ ← AI建议（可选）
└──────────────────────────────┘
```

#### 数据结构
```javascript
const similarityGroups = [
  {
    groupId: 'group_1',
    imageCount: 5,
    similarity: 0.95,  // 相似度
    images: [
      { id: 'img1', uri: '...' },
      { id: 'img2', uri: '...' },
      { id: 'img3', uri: '...' },
      // ...
    ],
    suggestion: {
      keep: 1,
      delete: 4
    }
  },
  // ... 最多8组
];
```

#### 交互
```javascript
onPress: (group) => {
  navigation.navigate('Category', {
    similarityGroupId: group.groupId,
    fromScreen: 'SimilarityGroup'
  });
}
```

#### "查看全部"功能
```javascript
onPressViewAll: () => {
  // 方案A: 展开显示所有相似组
  setExpandedSimilarityGroups(true);
  
  // 方案B: 跳转到独立页面（可选）
  // navigation.navigate('AllSimilarityGroups');
}
```

---

### 4. 按城市分类区

#### 布局
- 垂直列表
- **显示规则**: 显示所有有图片的城市（有多少显示多少）
- 按图片数量降序排列
- 列表项高度：44-50px

#### 城市列表项组成
```
┌──────────────────────────────┐
│ > 北京                 (128) │
├──────────────────────────────┤
│ > 上海                  (56) │
├──────────────────────────────┤
│ > 广州                  (34) │
└──────────────────────────────┘
```

#### 数据结构
```javascript
const cities = [
  {
    name: '北京',
    count: 128
  },
  {
    name: '上海',
    count: 56
  },
  // ... 所有城市
];
```

#### 交互
```javascript
onPress: (city) => {
  navigation.navigate('Category', {
    city: city.name,
    fromScreen: 'Home'
  });
}
```

---

### 5. 最近照片区

#### 布局
- 3列网格（Grid）
- **显示规则**: 固定显示最近12张图片（3列 x 4行）
- 按时间倒序（最新的在前）
- 图片尺寸：正方形，占屏幕宽度的1/3（减去间距）

#### 网格布局
```
┌───┐┌───┐┌───┐
│ 1 ││ 2 ││ 3 │ ← 第1行
└───┘└───┘└───┘
┌───┐┌───┐┌───┐
│ 4 ││ 5 ││ 6 │ ← 第2行
└───┘└───┘└───┘
┌───┐┌───┐┌───┐
│ 7 ││ 8 ││ 9 │ ← 第3行
└───┘└───┘└───┘
┌───┐┌───┐┌───┐
│10 ││11 ││12 │ ← 第4行
└───┘└───┘└───┘
```

#### 数据来源
```javascript
const recentImages = await UnifiedDataService.readRecentImages(12);
```

#### 交互
```javascript
onPress: (image, index) => {
  navigation.navigate('ImagePreview', {
    image: image,
    allImages: recentImages,
    currentIndex: index,
    fromScreen: 'Home'
  });
}
```

---

### 6. FAB扫描按钮

#### 位置
- 固定在屏幕右下角
- 浮动在内容之上
- 距离底部：80px（避开Tab栏）
- 距离右侧：16px

#### 样式
```javascript
{
  size: 56,          // 直径56px
  icon: '🔄',
  backgroundColor: '#007AFF',
  elevation: 6,      // Android阴影
  shadowOpacity: 0.3 // iOS阴影
}
```

#### 功能
```javascript
onPress: async () => {
  // 触发相册扫描
  await GalleryScannerService.startScan();
  
  // 显示扫描进度
  showScanProgress();
}
```

---

### 数据加载策略

#### 加载优先级

**第一优先级（立即加载）**:
```javascript
await Promise.all([
  loadStatistics(),      // 统计数字
  loadCategories(),      // 分类列表
  loadRecentImages(12)   // 最近12张
]);
```

**第二优先级（延迟100ms）**:
```javascript
setTimeout(() => {
  Promise.all([
    loadSimilarityGroups(8),  // 相似组（前8个）
    loadCities()              // 城市列表
  ]);
}, 100);
```

**第三优先级（滚动到可见时）**:
```javascript
// 分类代表图（懒加载）
onScrollToCategory: () => {
  loadCategoryThumbnails();
}

// 相似组缩略图（懒加载）
onScrollToSimilarity: () => {
  loadSimilarityThumbnails();
}
```

#### 刷新策略

**下拉刷新**:
```javascript
onRefresh: async () => {
  setRefreshing(true);
  await loadAllData();
  setRefreshing(false);
}
```

**自动刷新（扫描完成后）**:
```javascript
// 监听扫描完成事件
GalleryScannerService.on('scanComplete', () => {
  loadAllData();
});
```

---

## 📦 StagingBoxScreen（暂存箱）

### 实现方式
复用 CategoryScreen，传入特殊参数：
```javascript
<Tab.Screen 
  name="StagingBox" 
  component={CategoryScreen}
  initialParams={{
    category: 'tobecleaned',
    fromScreen: 'StagingBox'
  }}
/>
```

### 特殊UI
- 标题：📦 暂存箱
- Tab图标显示红点（有图片时）
- 操作按钮：
  - 🗑️ 永久删除（红色）
  - 📤 导出
  - 🔙 移出暂存箱

详细设计见 CategoryScreen 章节。

---

## 📁 CategoryScreen（分类详情页）

### 功能概述
通用列表页，根据传入参数显示不同内容：
- 按分类查看
- 按城市查看
- 按相似组查看
- 暂存箱管理

### 路由参数

```javascript
{
  // 三选一（必须）
  category?: string,           // 分类ID
  city?: string,               // 城市名
  similarityGroupId?: string,  // 相似组ID
  
  // 可选参数
  fromScreen: string,          // 来源页面
}
```

### 四种形态

#### 1. 普通分类页
```
参数: { category: 'screenshot' }
标题: 🖼️ 微信截图
操作: [多选] [放入暂存箱] [删除]
```

#### 2. 暂存箱页
```
参数: { category: 'tobecleaned' }
标题: 📦 暂存箱
操作: [多选] [永久删除] [移出暂存箱] [导出]
```

#### 3. 城市分类页
```
参数: { city: '北京' }
标题: 🏙️ 北京
操作: [多选] [放入暂存箱] [删除]
```

#### 4. 相似组详情页
```
参数: { similarityGroupId: 'group_1' }
标题: 🔍 相似组 1 (95%相似)
操作: [多选] [保留选中] [删除其他] [全部删除]
```

### 布局结构

```
┌────────────────────────────────┐
│ ← 返回  微信截图        ⋮ 更多  │ ← 顶部导航栏
├────────────────────────────────┤
│ 📊 45张 | 今日+5 | 已选0       │ ← 信息栏（可选）
├────────────────────────────────┤
│                                │
│ 📅 2025年1月 (10张)           │ ← 时间轴标题
│ ┌───┐┌───┐┌───┐              │
│ │   ││   ││   │              │
│ └───┘└───┘└───┘              │ ← 3列网格
│ ┌───┐┌───┐┌───┐              │
│ │   ││   ││   │              │
│ └───┘└───┘└───┘              │
│                                │
│ 📅 2024年12月 (15张)          │
│ ┌───┐┌───┐┌───┐              │
│ │   ││   ││   │              │
│ └───┘└───┘└───┘              │
│ ... 滚动加载更多                │
│                                │
├────────────────────────────────┤
│ [📦暂存] [📤导出] [🗑️删除]     │ ← 底部操作栏（选中时显示）
└────────────────────────────────┘
```

### 核心功能

#### 1. 图片网格显示
- 3列布局
- 响应式高度（保持正方形）
- 懒加载（虚拟列表）
- 缩略图优化

#### 2. 时间轴分组
```javascript
// 按日期分组
const groupedImages = {
  '2025-01': [/* 1月的图片 */],
  '2024-12': [/* 12月的图片 */],
  // ...
};
```

#### 3. 多选模式
- 长按进入多选模式
- 显示复选框
- 底部操作栏显示
- 支持全选/取消全选

#### 4. 批量操作

**普通分类 / 城市页**:
```javascript
操作选项：
- 📦 放入暂存箱
- 🗑️ 删除
- 📤 导出
```

**暂存箱页**:
```javascript
操作选项：
- 🗑️ 永久删除（红色警告）
- 🔙 移出暂存箱
- 📤 导出
```

**相似组页**:
```javascript
操作选项：
- ✅ 保留选中
- 🗑️ 删除其他
- 🗑️ 全部删除
```

### 数据加载

#### 分页加载
```javascript
const ITEMS_PER_PAGE = 50;

// 初始加载
const loadImages = async (page = 1) => {
  const images = await UnifiedDataService.readImagesByCategory(
    category, 
    page, 
    ITEMS_PER_PAGE
  );
  setImages(prev => [...prev, ...images]);
};

// 滚动到底部时加载更多
onEndReached: () => {
  if (hasMore) {
    loadImages(currentPage + 1);
  }
}
```

---

## 🖼️ ImagePreviewScreen（图片预览页）

### 功能概述
全屏显示图片，支持左右滑动切换，提供图片信息和操作按钮。

### 布局结构

```
┌────────────────────────────────┐
│ ← 返回                    ⋮    │ ← 顶部导航栏（半透明）
├────────────────────────────────┤
│                                │
│                                │
│         [大图显示区域]          │ ← 支持缩放、左右滑动
│                                │
│                                │
├────────────────────────────────┤
│ 📊 图片信息栏（可折叠）         │
│ 📅 2025-01-15 10:30           │
│ 📏 1920x1080 | 💾 2.3MB       │
│ 🏷️ 微信截图                   │
│ 📍 北京市朝阳区                │
└────────────────────────────────┘
│ [🗑️删除] [📦暂存] [🏷️分类] [📤分享] │ ← 底部操作栏
└────────────────────────────────┘
```

### 路由参数

```javascript
{
  image: object,               // 当前图片对象（必须）
  allImages?: array,           // 当前上下文的所有图片（可选）
  currentIndex?: number,       // 当前图片索引（可选）
  fromScreen: string,          // 来源页面
  
  // 上下文参数（用于返回）
  category?: string,
  city?: string,
  similarityGroupId?: string,
}
```

### 核心功能

#### 1. 图片查看
```javascript
// 左右滑动切换
<Swiper 
  index={currentIndex}
  onIndexChanged={(index) => setCurrentIndex(index)}
>
  {allImages.map(image => (
    <Image 
      source={{ uri: image.uri }}
      resizeMode="contain"
    />
  ))}
</Swiper>

// 双击缩放
onDoubleTap: () => {
  toggleZoom();
}

// 捏合缩放
<PinchGestureHandler>
  <Image ... />
</PinchGestureHandler>
```

#### 2. 图片信息
```javascript
const imageInfo = {
  fileName: image.fileName,
  size: formatFileSize(image.size),
  dimensions: `${image.width}x${image.height}`,
  category: getCategoryDisplayName(image.category),
  takenAt: formatDate(image.takenAt),
  location: formatLocation(image.latitude, image.longitude),
  city: image.city,
};
```

#### 3. 操作按钮

**删除**:
```javascript
onDelete: () => {
  Alert.alert(
    '确认删除',
    '确定要删除这张图片吗？',
    [
      { text: '取消', style: 'cancel' },
      { 
        text: '删除', 
        style: 'destructive',
        onPress: async () => {
          await UnifiedDataService.deleteImage(image.id);
          goBack();
        }
      }
    ]
  );
}
```

**放入暂存箱**:
```javascript
onStaging: async () => {
  await UnifiedDataService.updateImageCategory(
    image.id, 
    'tobecleaned'
  );
  showToast('已放入暂存箱');
  goBack();
}
```

**重新分类**:
```javascript
onReclassify: () => {
  showCategoryPicker((newCategory) => {
    await UnifiedDataService.updateImageCategory(
      image.id, 
      newCategory
    );
    showToast('分类已更新');
  });
}
```

**分享**:
```javascript
onShare: async () => {
  await Share.open({
    url: image.uri,
    type: 'image/*'
  });
}
```

---

## ⚙️ SettingsScreen（设置页）

### 布局结构

```
┌────────────────────────────────┐
│ 设置                           │ ← 标题栏
├────────────────────────────────┤
│                                │
│ 📁 扫描设置                    │
│ > 扫描路径                     │
│ > 自动扫描                     │
│                                │
│ 💾 存储管理                    │
│ > 缓存大小: 234MB             │
│ > 清理缓存                     │
│ > 数据备份                     │
│                                │
│ 🎨 显示设置                    │
│ > 主题模式                     │
│ > 图片质量                     │
│                                │
│ ℹ️ 关于                        │
│ > 版本信息: v1.0.0            │
│ > 使用帮助                     │
│ > 隐私政策                     │
│                                │
└────────────────────────────────┘
```

### 功能列表

#### 1. 扫描设置
- 选择扫描路径（相册权限）
- 自动扫描开关
- 扫描频率设置

#### 2. 存储管理
- 显示缓存大小
- 清理缓存按钮
- 数据备份/恢复

#### 3. 显示设置
- 主题模式（浅色/深色/自动）
- 图片质量（高/中/低）
- 网格列数设置

#### 4. 关于
- 版本信息
- 使用帮助
- 隐私政策
- 开源许可

---

## 🔄 页面跳转流程

### 跳转关系图

```
HomeScreen
│
├─ 点击暂存箱卡片 → Tab切换到 StagingBox
├─ 点击相似组卡片 → 滚动到相似组区域
├─ 点击分类卡片 → CategoryScreen(category)
├─ 点击相似组 → CategoryScreen(similarityGroupId)
├─ 点击城市 → CategoryScreen(city)
└─ 点击最近照片 → ImagePreviewScreen

StagingBox (CategoryScreen)
│
├─ 点击图片 → ImagePreviewScreen(tobecleaned)
└─ 点击返回 → 返回上一页

CategoryScreen
│
├─ 点击图片 → ImagePreviewScreen
└─ 点击返回 → 返回上一页

ImagePreviewScreen
│
└─ 点击返回 → 返回来源页面
```

### 导航代码示例

```javascript
// 从首页跳转到分类页
navigation.navigate('Category', {
  category: 'screenshot',
  fromScreen: 'Home'
});

// 从分类页跳转到预览页
navigation.navigate('ImagePreview', {
  image: currentImage,
  allImages: filteredImages,
  currentIndex: index,
  category: 'screenshot',
  fromScreen: 'Category'
});

// 返回上一页
navigation.goBack();

// 切换Tab
navigation.navigate('StagingBox');
```

---

## 🎨 UI设计规范

### 颜色方案

```javascript
const colors = {
  // 主色调
  primary: '#007AFF',      // iOS蓝
  secondary: '#5856D6',    // 紫色
  
  // 功能色
  success: '#34C759',      // 绿色（成功）
  warning: '#FF9500',      // 橙色（警告）
  danger: '#FF3B30',       // 红色（危险）
  info: '#AF52DE',         // 紫色（信息）
  
  // 灰度
  background: '#F2F2F7',   // 背景色
  surface: '#FFFFFF',      // 卡片背景
  border: '#E5E5EA',       // 边框
  text: '#000000',         // 主文本
  textSecondary: '#8E8E93', // 次要文本
  
  // 半透明
  overlay: 'rgba(0,0,0,0.5)', // 遮罩
  cardShadow: 'rgba(0,0,0,0.1)', // 卡片阴影
};
```

### 字体规范

```javascript
const typography = {
  // 标题
  h1: { fontSize: 28, fontWeight: 'bold' },
  h2: { fontSize: 22, fontWeight: 'bold' },
  h3: { fontSize: 18, fontWeight: '600' },
  
  // 正文
  body: { fontSize: 16, fontWeight: 'normal' },
  bodySmall: { fontSize: 14, fontWeight: 'normal' },
  
  // 标签
  caption: { fontSize: 12, fontWeight: 'normal' },
  
  // 按钮
  button: { fontSize: 16, fontWeight: '600' },
};
```

### 间距规范

```javascript
const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
};
```

### 圆角规范

```javascript
const borderRadius = {
  sm: 4,
  md: 8,
  lg: 12,
  xl: 16,
  full: 9999, // 完全圆角
};
```

### 阴影规范

```javascript
// iOS阴影
const iosShadow = {
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 2 },
  shadowOpacity: 0.1,
  shadowRadius: 4,
};

// Android阴影
const androidShadow = {
  elevation: 4,
};
```

---

## 📐 组件规范

### 统计卡片（StatCard）

```javascript
<StatCard
  icon="📸"           // 图标
  title="总图片"      // 标题
  value={256}         // 数值
  color="#007AFF"     // 主题色
  badge={false}       // 是否显示红点
  onPress={null}      // 点击回调
/>

// 尺寸
width: 45%          // 屏幕宽度的45%
height: 90px        // 固定高度
padding: 12px       // 内边距
borderRadius: 12px  // 圆角
```

### 分类卡片（CategoryCard）

```javascript
<CategoryCard
  icon="🖼️"
  name="微信截图"
  count={45}
  thumbnail="file://..."
  onPress={handlePress}
/>

// 尺寸
width: 110px
height: 150px
padding: 8px
borderRadius: 8px

// 缩略图
thumbnailSize: 80px
thumbnailBorderRadius: 8px
```

### 相似组卡片（SimilarityGroupCard）

```javascript
<SimilarityGroupCard
  groupId="group_1"
  imageCount={5}
  similarity={0.95}
  thumbnails={[...]}
  onPress={handlePress}
/>

// 尺寸
width: 100%
height: 90px
padding: 12px
borderRadius: 8px

// 缩略图
thumbnailSize: 60px
thumbnailCount: 3   // 最多显示3张
```

### 图片网格项（ImageGridItem）

```javascript
<ImageGridItem
  image={imageData}
  isSelected={false}
  onPress={handlePress}
  onLongPress={handleLongPress}
/>

// 尺寸
width: screenWidth / 3 - spacing
height: width           // 正方形
borderRadius: 4px

// 选中状态
checkboxSize: 24px
checkboxPosition: { top: 4, right: 4 }
selectedBorder: 2px #007AFF
```

---

## 📊 性能优化

### 图片加载优化

```javascript
// 使用缩略图
const thumbnailUri = image.uri + '?thumbnail=true';

// 懒加载
<Image 
  source={{ uri: image.uri }}
  loadingIndicatorSource={require('./placeholder.png')}
/>

// 缓存策略
<Image 
  source={{ uri: image.uri }}
  cache="force-cache"  // 强制缓存
/>
```

### 列表优化

```javascript
// 使用FlatList
<FlatList
  data={images}
  renderItem={renderItem}
  keyExtractor={item => item.id}
  
  // 性能优化
  initialNumToRender={12}      // 初始渲染数量
  maxToRenderPerBatch={12}     // 每批渲染数量
  windowSize={5}               // 渲染窗口大小
  removeClippedSubviews={true} // 移除不可见视图
  
  // 分页加载
  onEndReached={loadMore}
  onEndReachedThreshold={0.5}
/>
```

### 状态管理优化

```javascript
// 使用 React.memo 避免不必要的重渲染
const ImageItem = React.memo(({ image, onPress }) => {
  // ...
}, (prevProps, nextProps) => {
  return prevProps.image.id === nextProps.image.id &&
         prevProps.isSelected === nextProps.isSelected;
});

// 使用 useMemo 缓存计算结果
const filteredImages = useMemo(() => {
  return images.filter(img => img.category === category);
}, [images, category]);

// 使用 useCallback 缓存函数
const handleImagePress = useCallback((image) => {
  navigation.navigate('ImagePreview', { image });
}, [navigation]);
```

---

## 🔐 权限管理

### Android权限

```javascript
// 需要的权限
const permissions = [
  PermissionsAndroid.PERMISSIONS.READ_EXTERNAL_STORAGE,
  PermissionsAndroid.PERMISSIONS.WRITE_EXTERNAL_STORAGE,
  PermissionsAndroid.PERMISSIONS.CAMERA,  // 可选
];

// Android 13+
if (Platform.Version >= 33) {
  permissions.push(
    PermissionsAndroid.PERMISSIONS.READ_MEDIA_IMAGES
  );
}

// 请求权限
const requestPermissions = async () => {
  const granted = await PermissionsAndroid.requestMultiple(permissions);
  return Object.values(granted).every(
    status => status === PermissionsAndroid.RESULTS.GRANTED
  );
};
```

### iOS权限

```javascript
// Info.plist 配置
<key>NSPhotoLibraryUsageDescription</key>
<string>需要访问相册以进行图片分类整理</string>

<key>NSPhotoLibraryAddUsageDescription</key>
<string>需要保存导出的图片到相册</string>
```

---

## 📱 设备适配

### 屏幕尺寸适配

```javascript
import { Dimensions, PixelRatio } from 'react-native';

const { width: SCREEN_WIDTH, height: SCREEN_HEIGHT } = Dimensions.get('window');

// 基准宽度（iPhone 6/7/8）
const BASE_WIDTH = 375;

// 缩放比例
const scale = SCREEN_WIDTH / BASE_WIDTH;

// 适配函数
const normalize = (size) => {
  const newSize = size * scale;
  return Math.round(PixelRatio.roundToNearestPixel(newSize));
};

// 使用
const styles = StyleSheet.create({
  text: {
    fontSize: normalize(16),  // 适配后的字体大小
  }
});
```

### 安全区域适配

```javascript
import { SafeAreaView } from 'react-native-safe-area-context';

<SafeAreaView style={styles.container}>
  {/* 内容会自动避开刘海、底部横条等 */}
</SafeAreaView>
```

---

## 🧪 测试要点

### 功能测试

- [ ] 统计数据正确显示
- [ ] 分类卡片正确加载
- [ ] 相似组正确识别
- [ ] 城市定位准确
- [ ] 图片预览流畅
- [ ] 多选模式正常
- [ ] 批量操作成功
- [ ] 暂存箱功能正常
- [ ] 扫描功能正常
- [ ] 权限请求正常

### 性能测试

- [ ] 首页加载时间 < 2秒
- [ ] 滚动流畅（60fps）
- [ ] 图片加载不卡顿
- [ ] 大量图片（1000+）性能良好
- [ ] 内存占用合理（< 200MB）

### 兼容性测试

- [ ] Android 10-14 测试
- [ ] iOS 14-17 测试
- [ ] 不同屏幕尺寸适配
- [ ] 横竖屏切换正常
- [ ] 深色模式适配

---

## 📋 待实现功能（Future）

### Phase 2
- [ ] 搜索功能
- [ ] 图片编辑（裁剪、滤镜）
- [ ] 批量导出到相册
- [ ] 云同步

### Phase 3
- [ ] AI智能推荐
- [ ] 自动整理建议
- [ ] 批量重命名
- [ ] 高级筛选

---

## 📝 更新日志

### v2.0 (2025-01-20)
- 🎉 初始移动端设计方案
- 📱 基于PC端功能完整适配
- 🎨 Material Design + iOS Human Interface Guidelines
- ⚡ 性能优化和懒加载策略

---

## 📞 联系信息

- **项目地址**: [GitHub仓库链接]
- **设计文档**: docs/design/
- **技术文档**: docs/architecture/

---

**最后更新**: 2025-01-20  
**文档版本**: v2.0  
**维护者**: Development Team

