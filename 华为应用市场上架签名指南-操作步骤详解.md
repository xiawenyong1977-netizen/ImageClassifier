# 华为应用市场上架签名 - 操作步骤详解

## ⚠️ 重要说明

**您看到的"证书"页面不是用来创建上传密钥的！**

- 📋 **证书页面**：用于创建调试证书、发布证书（主要用于iOS或其他特定场景）
- 🔐 **上传密钥**：需要在"应用签名"或"签名密钥管理"部分配置
- 🎯 **正确位置**：应用详情 → 版本信息 → 签名密钥管理

---

## 🔍 找到正确的页面

### 正确路径

1. **登录 AppGallery Connect**
   - 访问：https://developer.huawei.com/consumer/cn/service/josp/agc/index.html

2. **进入应用详情**
   - 如果还没有创建应用，先创建应用
   - 点击应用名称进入应用详情页

3. **找到签名密钥管理**
   - 左侧菜单选择：**"版本信息"** 或 **"应用信息"**
   - 在页面中找到：**"签名密钥管理"** 或 **"应用签名"** 或 **"签名配置"**
   - **不是** "证书、APP ID和Profile" → "证书"

### 页面位置示意

```
AppGallery Connect
├── 我的应用
│   └── [您的应用名称]
│       ├── 版本信息  ← 在这里
│       │   ├── 签名密钥管理  ← 正确位置！
│       │   └── 新建版本
│       └── 应用信息
│           └── 签名配置  ← 也可能在这里
└── 证书、APP ID和Profile  ← 这个不是！
    └── 证书  ← 不是这里！
```

---

## 📝 方式二操作步骤（详细版）

### 步骤1：准备签名密钥

**如果没有签名密钥，先生成一个**：

```powershell
cd android
.\generate-upload-key.ps1
```

**如果已有签名密钥**（如 `android-release-key.keystore`），直接使用。

### 步骤2：下载pepk.jar工具

**在 AppGallery Connect 中下载**：

1. 登录 AppGallery Connect
2. 进入应用详情 → 版本信息
3. 找到"签名密钥管理"或"应用签名"部分
4. 查看是否有"下载pepk.jar"或"工具下载"链接
5. 下载 `pepk.jar` 文件

**如果找不到下载链接**：

- 方式1：联系华为开发者支持
- 方式2：在华为开发者社区搜索"pepk.jar下载"
- 方式3：先创建应用并上传APK，系统可能会提示下载

**将pepk.jar放到项目目录**：
```powershell
# 将下载的pepk.jar放到android目录
Copy-Item "下载路径\pepk.jar" "D:\ImageClassifierApp\android\"
```

### 步骤3：打包签名密钥

```powershell
cd android

# 使用现有密钥
.\package-sign-key-for-agc.ps1 -KeystorePath "android-release-key.keystore" -Alias "imageclassifier"

# 或使用新生成的密钥
.\package-sign-key-for-agc.ps1 -KeystorePath "upload-keystore.jks" -Alias "upload"
```

**输入密码后，会生成 `sign.zip` 文件**

### 步骤4：上传到AGC

1. **进入正确的页面**
   - AppGallery Connect → 我的应用 → [您的应用] → **版本信息**
   - 找到 **"签名密钥管理"** 或 **"应用签名"**

2. **选择"由AGC创建并管理签名密钥"**

3. **上传sign.zip文件**
   - 点击上传按钮
   - 选择 `android/sign.zip` 文件
   - 等待系统验证和解密

4. **完成配置**
   - 系统会显示密钥配置成功
   - 后续可以直接上传未签名的APK

---

## 🎯 如果找不到pepk.jar下载链接

### 临时解决方案：使用方式一（最简单）

如果暂时无法获取pepk.jar，建议先使用方式一：

1. **登录 AppGallery Connect**
2. **创建应用**（如果还没有）
3. **直接上传APK**（未签名或debug签名都可以）
4. **AGC自动生成并管理签名密钥**
5. **完成上架**

**后续如果需要方式二**：
- 等待华为官方提供pepk.jar下载
- 或联系华为开发者支持获取

---

## 🔧 检查清单

在开始操作前，确认以下事项：

- [ ] 已登录 AppGallery Connect
- [ ] 已创建应用（如果还没有）
- [ ] 已进入应用详情页
- [ ] 已找到"版本信息" → "签名密钥管理"（不是"证书"页面）
- [ ] 已准备好签名密钥（现有或新生成）
- [ ] 已下载pepk.jar（方式二需要）
- [ ] 已将pepk.jar放到android目录

---

## 📞 获取帮助

如果遇到问题：

1. **华为开发者文档**
   - 在 AppGallery Connect 页面查找帮助文档
   - 搜索"应用签名"或"签名密钥"

2. **华为开发者支持**
   - 在 AppGallery Connect 页面查找"联系支持"或"帮助中心"

3. **开发者社区**
   - 华为开发者社区搜索相关问题

---

## ✅ 快速验证

**确认您是否在正确页面**：

- ✅ 页面标题包含"签名密钥"或"应用签名"
- ✅ 有"由AGC创建并管理签名密钥"选项
- ✅ 有上传sign.zip的按钮
- ❌ 不是"证书"页面
- ❌ 不是"调试证书"或"发布证书"

---

**最后更新**：2025-01-24

