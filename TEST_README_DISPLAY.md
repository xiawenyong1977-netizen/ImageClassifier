# 测试 Readme 显示功能

## 方法1: 清空数据库（推荐用于真实测试）

如果您想测试首次启动时的 readme 显示效果：

1. 关闭应用
2. 删除或重命名数据库文件（通常在用户数据目录中）
3. 重新启动应用

数据库文件位置：
- Windows: `%APPDATA%/芯图相册/` 或类似目录
- 查找文件名包含 `.db` 或 `.sqlite` 的文件

## 方法2: 使用URL参数强制显示（开发测试）

在应用运行时，打开开发者工具（F12），在控制台输入：

```javascript
window.location.href = '?showReadme=true'
```

或者修改地址栏URL，添加参数 `?showReadme=true`

## 方法3: 直接测试（推荐）

现在 readme 文件和图片已经复制到 `public/readme/` 目录中，应用会优先从该目录加载：

1. 直接启动应用（确保图片数量为 0）
2. 应该能看到完整的 readme 内容显示，包括所有图片
3. 如果仍然显示 fallback 内容，请检查控制台日志

## 文件结构

```
public/
└── readme/
    ├── readme.md
    ├── 进入设置页面.jpg
    ├── 设置照片目录并智能分类.jpg
    ├── 分类进展和统计信息.jpg
    ├── 暂存.jpg
    ├── 进入暂存箱.jpg
    ├── 暂存箱处置.jpg
    ├── 首页-1.jpg
    └── 首页-2.jpg
```

## 查看调试信息

打开开发者工具（F12），查看Console标签，会显示：
- 图片总数
- readme 文件读取状态
- 显示条件判断结果

## 预期效果

当图片总数为 0 或者使用 `?showReadme=true` 参数时，应该显示：

1. readme.md 的完整内容
2. 包含所有插入的图片（水平排列或单张）
3. 底部有"进入设置 →"按钮
4. 点击按钮会跳转到设置页面

## 如果 readme 不显示

检查控制台日志，查看：
- `totalImagesCount` 的值
- `readmeContent` 的长度
- readme 文件的读取路径是否正确
- 是否有错误信息

常见问题：
- 如果 readme 文件找不到，会显示简单的欢迎文本
- 如果图片总数不为 0，需要使用 URL 参数强制显示

