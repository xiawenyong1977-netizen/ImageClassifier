# 华为应用市场上架签名 - 快速开始指南

## 🚀 快速决策树

```
您有签名密钥吗？
├─ 有 ✅
│  └─ 使用现有密钥 → 跳到"打包密钥"步骤
│
└─ 没有 ❌
   └─ 选择生成方式：
      ├─ 方式1：使用项目现有密钥（android-release-key.keystore）
      │  └─ 别名：imageclassifier
      │  └─ 需要密码（查看项目配置）
      │
      └─ 方式2：生成新密钥（推荐，约1分钟）
         └─ 运行脚本：.\generate-upload-key.ps1
```

---

## 📋 场景1：我没有签名密钥

### 步骤1：生成签名密钥（1分钟）

```powershell
cd android
.\generate-upload-key.ps1
```

**按提示输入**：
- 密钥库密码（建议使用强密码）
- 密钥密码（可与密钥库密码相同）
- 密钥别名（默认：upload）

**生成的文件**：
- `upload-keystore.jks` - 签名密钥文件
- `upload_certificate.pem` - 证书文件

### 步骤2：选择上架方式

#### 方式A：仅上传证书（简单）

```powershell
# 证书已生成，直接上传 upload_certificate.pem 到AGC
# 后续需要手动签名APK
```

#### 方式B：上传加密密钥（推荐，由AGC管理）

```powershell
# 1. 下载pepk.jar（如果还没有）
# 从华为开发者文档下载

# 2. 打包密钥
.\package-sign-key-for-agc.ps1 -KeystorePath "upload-keystore.jks" -Alias "upload"

# 3. 上传 sign.zip 到AGC
# 后续可以直接上传未签名的APK，AGC自动签名
```

---

## 📋 场景2：我有签名密钥

### 如果使用项目现有密钥（android-release-key.keystore）

```powershell
cd android

# 方式A：仅上传证书
keytool -export -rfc -keystore android-release-key.keystore -alias imageclassifier -file upload_certificate.pem
# 输入密码后，上传 upload_certificate.pem 到AGC

# 方式B：打包加密密钥（推荐）
.\package-sign-key-for-agc.ps1 -KeystorePath "android-release-key.keystore" -Alias "imageclassifier"
# 输入密码后，上传 sign.zip 到AGC
```

### 如果使用其他密钥文件

```powershell
cd android

# 替换为您的密钥文件路径和别名
.\package-sign-key-for-agc.ps1 -KeystorePath "您的密钥文件.jks" -Alias "您的别名"
```

---

## 📋 场景3：我想使用方式一（最简单）

**无需签名密钥！**

1. 登录 AppGallery Connect
2. 创建应用
3. 直接上传APK（未签名或debug签名都可以）
4. AGC自动生成并管理签名密钥
5. 完成！

**优点**：最简单，无需任何配置  
**缺点**：密钥无法导出，不适合多平台分发

---

## ✅ 推荐流程（没有密钥的情况下）

### 最佳实践：生成新密钥 + 方式B（AGC管理）

```powershell
# 1. 生成签名密钥（1分钟）
cd android
.\generate-upload-key.ps1

# 2. 下载pepk.jar工具
# 访问：https://developer.huawei.com/consumer/cn/doc/development/AppGallery-connect-Guides/agc-appsigning-pepk
# 将pepk.jar放到android目录

# 3. 打包密钥（30秒）
.\package-sign-key-for-agc.ps1 -KeystorePath "upload-keystore.jks" -Alias "upload"

# 4. 上传sign.zip到AGC（在网页上操作）
# 5. 后续直接上传未签名的APK，AGC自动签名
```

**总耗时**：约2-3分钟  
**后续更新**：直接上传APK即可，无需签名

---

## 🔧 常见问题

### Q: 我没有签名密钥，必须生成吗？

**A**: 不一定。可以选择：
- ✅ **方式一**：由AGC管理，无需签名密钥
- ✅ **方式二**：生成新密钥（推荐，约1分钟）

### Q: 可以使用现有的 android-release-key.keystore 吗？

**A**: 可以！但需要知道密码和别名。
- 密钥文件：`android/android-release-key.keystore`
- 别名：`imageclassifier`
- 密码：需要查看项目配置或询问维护者

### Q: 生成新密钥后，会影响现有应用吗？

**A**: 不会。这是新的密钥，用于新上架的应用。如果您已经在其他平台使用了 `android-release-key.keystore`，可以继续使用它，也可以为华为单独创建新密钥。

### Q: 方式B上传加密密钥后，后续还需要签名吗？

**A**: 不需要！AGC会使用您上传的密钥自动签名。后续更新时：
- 直接上传未签名的APK
- AGC自动使用您上传的密钥签名
- 无需任何手动操作

---

## 📚 相关文档

- [完整签名指南](华为应用市场上架签名指南.md) - 详细操作说明
- [自动生成密钥脚本](android/generate-upload-key.ps1)
- [打包密钥脚本](android/package-sign-key-for-agc.ps1)

---

**最后更新**：2025-01-24

