# 华为应用市场上架签名密钥操作指南

## 📋 概述

华为应用市场提供了两种签名密钥管理方式：

### 方式一：由AppGallery Connect创建并管理（推荐首次上架）

**适用场景**：应用首次上架华为应用市场

**优点**：
- ✅ 操作简单，无需自己管理密钥
- ✅ AGC自动生成并保存签名密钥
- ✅ 后续更新自动使用相同密钥签名

**缺点**：
- ❌ 无法查看或导出密钥
- ❌ 不适合需要同时上架多个应用商店的场景
- ❌ 无法用于对接第三方平台

### 方式二：自己提供上传密钥（推荐多平台分发）

**适用场景**：
- 需要同时上架多个应用商店（华为、小米、OPPO、vivo等）
- 需要对接第三方平台
- 需要自主管理签名密钥

**优点**：
- ✅ 可以导出和管理密钥
- ✅ 适合多平台分发
- ✅ 可以对接第三方平台

**缺点**：
- ❌ 需要自己管理密钥安全
- ❌ 操作步骤相对复杂

---

## 🎯 推荐方案

**如果您计划只上架华为应用市场**：选择**方式一**（由AGC管理）

**如果您计划上架多个应用商店**：选择**方式二**（自己提供上传密钥）

**如果您没有签名密钥**：
- ✅ **不用担心！** 可以使用现有密钥或快速生成新密钥
- 📝 项目中已有 `android-release-key.keystore`（别名：`imageclassifier`），可以直接使用
- 🔧 或者使用脚本快速生成新的上传密钥（约1分钟）

---

## 📝 方式一：由AGC创建并管理签名密钥

### 操作步骤

1. **登录AppGallery Connect**
   - 访问：https://developer.huawei.com/consumer/cn/service/josp/agc/index.html
   - 登录华为开发者账号

2. **创建应用**
   - 进入"我的应用"
   - 点击"创建应用"
   - 填写应用基本信息

3. **上传APK**
   - 进入应用详情页
   - 选择"版本信息" → "新建版本"
   - 上传未签名的APK（或debug签名的APK）
   - **系统会自动生成签名密钥并签名**

4. **完成上架**
   - 填写应用详情
   - 提交审核

**注意**：AGC会自动保存签名密钥，后续更新使用相同密钥签名，无需再次配置。

---

## 🔐 方式二：自己提供上传密钥（详细步骤）

### 前置准备

确保已安装JDK（Java Development Kit），通常Android Studio会自动安装。

### 步骤1：生成上传密钥文件（如果没有签名密钥）

> **如果您已有签名密钥**：可以跳过此步骤，直接使用现有密钥文件（如 `android-release-key.keystore`）

**方式A：使用现有密钥文件（推荐）**

项目中已有 `android-release-key.keystore` 文件，别名是 `imageclassifier`，可以直接使用：
- 密钥文件：`android/android-release-key.keystore`
- 别名：`imageclassifier`
- 密码：请查看项目配置文件或询问项目维护者

**方式B：生成新的上传密钥**

如果希望为华为应用市场单独创建密钥，在项目 `android` 目录下执行以下命令：

#### Windows (PowerShell)

```powershell
# 进入android目录
cd android

# 生成上传密钥（.jks格式）
keytool -genkeypair -v -storetype PKCS12 -keystore upload-keystore.jks -alias upload -keyalg RSA -keysize 2048 -validity 10000 -storepass 您的密钥库密码 -keypass 您的密钥密码

# 参数说明：
# -keystore upload-keystore.jks: 生成的密钥文件名称
# -alias upload: 密钥别名（固定为upload）
# -validity 10000: 有效期10000天（约27年）
# -storepass: 密钥库密码（请记住此密码）
# -keypass: 密钥密码（可以与密钥库密码相同）
```

#### macOS/Linux

```bash
cd android
keytool -genkeypair -v -storetype PKCS12 -keystore upload-keystore.jks -alias upload -keyalg RSA -keysize 2048 -validity 10000 -storepass 您的密钥库密码 -keypass 您的密钥密码
```

**示例命令**（请替换密码）：

```powershell
keytool -genkeypair -v -storetype PKCS12 -keystore upload-keystore.jks -alias upload -keyalg RSA -keysize 2048 -validity 10000 -storepass MyUploadPass123! -keypass MyUploadPass123!
```

**重要提示**：
- 🔒 **请妥善保管密钥文件（.jks）和密码**
- 📝 **建议将密码保存在安全的地方（如密码管理器）**
- 🚫 **不要将密钥文件提交到Git仓库**

### 步骤2：生成上传密钥证书（.pem文件）

#### Windows (PowerShell)

```powershell
# 确保在android目录下
cd android

# 导出证书（需要输入密钥库密码）
keytool -export -rfc -keystore upload-keystore.jks -alias upload -file upload_certificate.pem

# 或者使用完整路径指定JDK
# "C:\Program Files\Java\jdk-XX\bin\keytool" -export -rfc -keystore upload-keystore.jks -alias upload -file upload_certificate.pem
```

#### macOS/Linux

```bash
cd android
keytool -export -rfc -keystore upload-keystore.jks -alias upload -file upload_certificate.pem
```

**命令执行后会提示输入密钥库密码**，输入步骤1中设置的密码即可。

**输出**：
- 成功后会生成 `upload_certificate.pem` 文件
- 文件位置：`android/upload_certificate.pem`

### 步骤3：上传签名密钥到AGC（两种方式）

#### 方式A：仅上传证书（简单方式）

1. **登录AppGallery Connect**
   - 访问：https://developer.huawei.com/consumer/cn/service/josp/agc/index.html

2. **进入应用详情**
   - 选择您的应用
   - 进入"版本信息"

3. **配置签名密钥**
   - 选择"签名密钥管理"或"上传密钥配置"
   - 上传 `upload_certificate.pem` 文件

4. **验证配置**
   - 系统会验证证书格式
   - 验证通过后保存配置

#### 方式B：上传加密的签名密钥（推荐，由AGC管理）

**优点**：AGC将为您保管签名密钥，后续应用更新时自动使用此密钥签名，无需每次手动签名。

**前置准备**：
1. 下载 `pepk.jar` 工具
   - **方式1**：登录 AppGallery Connect → 进入应用详情 → 版本信息 → 签名密钥管理 → 下载 pepk.jar
   - **方式2**：访问华为开发者文档搜索"pepk.jar"或"应用签名"
   - **方式3**：在 AppGallery Connect 中创建应用后，在"应用签名"相关页面通常有下载链接
   - 将 `pepk.jar` 放到项目 `android` 目录下
   
   > **注意**：如果无法访问外部链接，可以在 AppGallery Connect 控制台内查找下载入口

**操作步骤**：

**Windows用户（使用自动化脚本）**：

```powershell
cd android

# 方式1：使用现有密钥（如果已有）
.\package-sign-key-for-agc.ps1 -KeystorePath "android-release-key.keystore" -Alias "imageclassifier"

# 方式2：使用新生成的密钥
# 先运行生成密钥脚本（如果没有）
.\generate-upload-key.ps1
# 然后打包
.\package-sign-key-for-agc.ps1 -KeystorePath "upload-keystore.jks" -Alias "upload"

# 如果没有pepk.jar，脚本会提示下载链接
```

**手动操作**（可在任意目录执行）：

```powershell
# 确保pepk.jar和签名密钥文件在同一目录，或使用完整路径

# 方式1：如果文件都在当前目录
java -jar pepk.jar --keystore sign-keystore.jks --alias sign --output=sign.zip --encryptionkey=034200041E224EE22B45D19B23DB91BA9F52DE0A06513E03A5821409B34976FDEED6E0A47DBA48CC249DD93734A6C5D9A0F43461F9E140F278A5D2860846C2CF5D2C3C02 --include-cert

# 方式2：使用完整路径（推荐）
java -jar "D:\ImageClassifierApp\android\pepk.jar" --keystore "D:\ImageClassifierApp\android\sign-keystore.jks" --alias sign --output="D:\ImageClassifierApp\android\sign.zip" --encryptionkey=034200041E224EE22B45D19B23DB91BA9F52DE0A06513E03A5821409B34976FDEED6E0A47DBA48CC249DD93734A6C5D9A0F43461F9E140F278A5D2860846C2CF5D2C3C02 --include-cert
```

**注意**：执行命令时会提示输入密钥库密码和密钥密码。

   **参数说明**：
   - `--keystore sign-keystore.jks`: 您的签名密钥文件
   - `--alias sign`: 密钥别名（替换为您实际的别名）
   - `--output=sign.zip`: 输出的加密密钥包
   - `--encryptionkey=...`: 华为提供的固定加密公钥（必须使用此值）
   - `--include-cert`: 包含证书

3. **上传sign.zip到AGC**
   - 登录 AppGallery Connect
   - 进入应用详情 → 版本信息 → 签名密钥管理
   - 选择"由AGC创建并管理签名密钥"
   - 上传 `sign.zip` 文件
   - AGC将解密并保存您的签名密钥

**重要提示**：
- ✅ 上传后，AGC将为您保管签名密钥
- ✅ 后续应用更新时，直接上传APK即可，AGC会自动使用此密钥签名
- ✅ 您无需在本地保存签名密钥（但建议备份）
- 🔒 `sign.zip` 文件已加密，可以安全上传

### 步骤4：使用签名密钥签名APK（仅方式A需要）

> **注意**：如果使用方式B（上传加密密钥），后续可以直接上传未签名的APK，AGC会自动签名。本节仅适用于方式A。

#### 方式A：使用Android Studio

1. 打开 `android/app/build.gradle`
2. 添加签名配置：

```gradle
android {
    // ... 其他配置 ...
    
    signingConfigs {
        release {
            storeFile file('../upload-keystore.jks')
            storePassword '您的密钥库密码'
            keyAlias 'upload'
            keyPassword '您的密钥密码'
        }
    }
    
    buildTypes {
        release {
            signingConfig signingConfigs.release
            // ... 其他配置 ...
        }
    }
}
```

3. 构建Release APK：
   ```bash
   cd android
   ./gradlew assembleRelease
   ```

#### 方式B：使用命令行签名（推荐）

**Windows (PowerShell)**：

```powershell
cd android

# 1. 构建未签名的APK
.\gradlew assembleRelease

# 2. 使用jarsigner签名（方法1）
jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA-256 -keystore upload-keystore.jks -storepass 您的密钥库密码 app\build\outputs\apk\release\app-release-unsigned.apk upload

# 3. 对齐APK（可选，但推荐）
zipalign -v 4 app\build\outputs\apk\release\app-release-unsigned.apk app\build\outputs\apk\release\app-release-signed.apk

# 4. 验证签名
jarsigner -verify -verbose -certs app\build\outputs\apk\release\app-release-signed.apk
```

**或者使用apksigner（Android SDK工具，推荐）**：

```powershell
# 使用apksigner签名（方法2，推荐）
# 路径通常在：C:\Users\您的用户名\AppData\Local\Android\Sdk\build-tools\XX.X.X\apksigner.bat

$apksigner = "C:\Users\$env:USERNAME\AppData\Local\Android\Sdk\build-tools\33.0.0\apksigner.bat"
& $apksigner sign --ks upload-keystore.jks --ks-key-alias upload --ks-pass pass:您的密钥库密码 --key-pass pass:您的密钥密码 --out app\build\outputs\apk\release\app-release-signed.apk app\build\outputs\apk\release\app-release-unsigned.apk

# 验证签名
& $apksigner verify --verbose app\build\outputs\apk\release\app-release-signed.apk
```

### 步骤5：上传APK到AGC

#### 方式A：上传已签名的APK

1. 登录AppGallery Connect
2. 进入应用详情 → 版本信息 → 新建版本
3. 上传已签名的APK：`app-release-signed.apk`
4. 系统会验证签名是否与上传的证书匹配
5. 验证通过后提交审核

#### 方式B：上传未签名的APK（推荐）

1. 登录AppGallery Connect
2. 进入应用详情 → 版本信息 → 新建版本
3. 上传未签名的APK（或debug签名的APK）
4. **AGC会自动使用您上传的签名密钥进行签名**
5. 验证通过后提交审核

**方式B的优势**：
- ✅ 无需每次手动签名
- ✅ AGC自动管理签名密钥
- ✅ 更安全，密钥由AGC保管
- ✅ 适合持续集成和自动化发布

---

## 🔧 当前项目配置建议

### 检查现有签名配置

您的项目中已有 `android-release-key.keystore` 文件，可以：

**选项1：使用现有密钥作为上传密钥**

如果 `android-release-key.keystore` 是您希望用于华为应用市场的密钥：

```powershell
cd android

# 导出证书
keytool -export -rfc -keystore android-release-key.keystore -alias 您的别名 -file upload_certificate.pem
```

**选项2：创建新的上传密钥（推荐）**

为华为应用市场单独创建上传密钥，便于管理：

```powershell
cd android

# 生成新的上传密钥
keytool -genkeypair -v -storetype PKCS12 -keystore upload-keystore.jks -alias upload -keyalg RSA -keysize 2048 -validity 10000 -storepass YourPassword123! -keypass YourPassword123!
```

### 配置build.gradle

在 `android/app/build.gradle` 中添加签名配置：

```gradle
android {
    // ... 现有配置 ...
    
    signingConfigs {
        release {
            // 如果使用现有密钥
            // storeFile file('../android-release-key.keystore')
            // storePassword System.getenv("KEYSTORE_PASSWORD") ?: "您的密码"
            // keyAlias "您的别名"
            // keyPassword System.getenv("KEY_PASSWORD") ?: "您的密码"
            
            // 如果使用新的上传密钥
            storeFile file('../upload-keystore.jks')
            storePassword System.getenv("UPLOAD_KEYSTORE_PASSWORD") ?: "您的密码"
            keyAlias 'upload'
            keyPassword System.getenv("UPLOAD_KEY_PASSWORD") ?: "您的密码"
        }
    }
    
    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled enableProguardInReleaseBuilds
            proguardFiles getDefaultProguardFile("proguard-android.txt"), "proguard-rules.pro"
        }
    }
}
```

---

## 🛡️ 安全建议

### 密钥文件安全

1. **不要提交到Git**
   - 在 `.gitignore` 中添加：
     ```
     *.keystore
     *.jks
     *.pem
     upload-keystore.jks
     upload_certificate.pem
     ```

2. **备份密钥文件**
   - 将密钥文件保存到安全位置（加密存储）
   - 建议使用密码管理器保存密码

3. **密码安全**
   - 使用强密码（至少16位，包含大小写字母、数字、特殊字符）
   - 不要共享密钥文件或密码

### 环境变量配置（推荐）

为了避免在代码中硬编码密码，建议使用环境变量：

**Windows PowerShell**：

```powershell
# 设置环境变量（当前会话）
$env:UPLOAD_KEYSTORE_PASSWORD = "您的密码"
$env:UPLOAD_KEY_PASSWORD = "您的密码"

# 永久设置（需要管理员权限）
[System.Environment]::SetEnvironmentVariable("UPLOAD_KEYSTORE_PASSWORD", "您的密码", "User")
```

**macOS/Linux**：

```bash
# 添加到 ~/.bashrc 或 ~/.zshrc
export UPLOAD_KEYSTORE_PASSWORD="您的密码"
export UPLOAD_KEY_PASSWORD="您的密码"
```

---

## 📝 快速操作脚本

### Windows PowerShell脚本

创建 `android/generate-upload-key.ps1`：

```powershell
# 生成华为应用市场上传密钥脚本

Write-Host "🔐 生成华为应用市场上传密钥..." -ForegroundColor Green

# 读取密码（避免在命令行中显示）
$keystorePassword = Read-Host "请输入密钥库密码" -AsSecureString
$keyPassword = Read-Host "请输入密钥密码（可与密钥库密码相同）" -AsSecureString

$keystorePasswordPlain = [Runtime.InteropServices.Marshal]::PtrToStringAuto([Runtime.InteropServices.Marshal]::SecureStringToBSTR($keystorePassword))
$keyPasswordPlain = [Runtime.InteropServices.Marshal]::PtrToStringAuto([Runtime.InteropServices.Marshal]::SecureStringToBSTR($keyPassword))

# 生成密钥
Write-Host "`n📦 步骤1: 生成上传密钥文件..." -ForegroundColor Yellow
keytool -genkeypair -v -storetype PKCS12 -keystore upload-keystore.jks -alias upload -keyalg RSA -keysize 2048 -validity 10000 -storepass $keystorePasswordPlain -keypass $keyPasswordPlain

if ($LASTEXITCODE -ne 0) {
    Write-Host "❌ 生成密钥失败" -ForegroundColor Red
    exit 1
}

# 导出证书
Write-Host "`n📄 步骤2: 导出证书文件..." -ForegroundColor Yellow
keytool -export -rfc -keystore upload-keystore.jks -alias upload -file upload_certificate.pem -storepass $keystorePasswordPlain

if ($LASTEXITCODE -ne 0) {
    Write-Host "❌ 导出证书失败" -ForegroundColor Red
    exit 1
}

Write-Host "`n✅ 完成！" -ForegroundColor Green
Write-Host "📁 密钥文件: upload-keystore.jks" -ForegroundColor Cyan
Write-Host "📄 证书文件: upload_certificate.pem" -ForegroundColor Cyan
Write-Host "`n⚠️  请妥善保管密钥文件和密码！" -ForegroundColor Yellow
Write-Host "📤 下一步：将 upload_certificate.pem 上传到AppGallery Connect" -ForegroundColor Cyan
```

---

## ❓ 常见问题

### Q1: 找不到keytool命令？

**A**: 确保JDK已安装并添加到PATH环境变量。或者使用完整路径：
```powershell
# Windows
"C:\Program Files\Java\jdk-XX\bin\keytool.exe" -version

# macOS (通常由Android Studio安装)
/Library/Java/JavaVirtualMachines/jdk-XX.jdk/Contents/Home/bin/keytool -version
```

### Q2: 证书文件格式错误？

**A**: 确保使用 `-rfc` 参数导出RFC格式的证书：
```bash
keytool -export -rfc -keystore upload-keystore.jks -alias upload -file upload_certificate.pem
```

### Q3: 签名验证失败？

**A**: 检查以下几点：
- 密钥别名是否正确（应为 `upload`）
- 密码是否正确
- APK是否未签名或签名损坏

### Q4: 如何查看密钥信息？

```bash
# 查看密钥库信息
keytool -list -v -keystore upload-keystore.jks

# 查看证书信息
keytool -printcert -file upload_certificate.pem
```

---

## 📚 相关文档

- [华为应用市场开发者文档](https://developer.huawei.com/consumer/cn/doc/development/AppGallery-connect-Guides/agc-appsigning-introduction)
- [Android应用签名指南](https://developer.android.com/studio/publish/app-signing)
- [keytool命令参考](https://docs.oracle.com/javase/8/docs/technotes/tools/unix/keytool.html)

---

---

## 🚀 快速开始（推荐流程）

### 方式一：由AGC管理（最简单）

1. 登录 AppGallery Connect
2. 创建应用并上传APK
3. 系统自动生成签名密钥
4. 完成！

### 方式二：自己提供上传密钥（5分钟完成）

**Windows用户**：

```powershell
# 1. 进入android目录
cd android

# 2. 运行自动生成脚本
.\generate-upload-key.ps1

# 3. 按照提示输入密码等信息

# 4. 上传 upload_certificate.pem 到AGC

# 5. 使用 upload-keystore.jks 签名APK后上传
```

**手动操作**：

```powershell
# 1. 生成密钥
keytool -genkeypair -v -storetype PKCS12 -keystore upload-keystore.jks -alias upload -keyalg RSA -keysize 2048 -validity 10000 -storepass 您的密码 -keypass 您的密码

# 2. 导出证书
keytool -export -rfc -keystore upload-keystore.jks -alias upload -file upload_certificate.pem -storepass 您的密码

# 3. 上传 upload_certificate.pem 到AGC
```

---

**最后更新**：2025-01-24

**适用版本**：芯图相册 v2.0

**相关文件**：
- `android/generate-upload-key.ps1` - 自动生成上传密钥脚本
- `android/package-sign-key-for-agc.ps1` - 打包加密签名密钥脚本（用于AGC管理）
- `华为应用市场上架签名指南.md` - 本文档

**快速参考**：

```powershell
# 1. 生成签名密钥（如果还没有）
cd android
.\generate-upload-key.ps1

# 2. 打包密钥用于AGC（方式B推荐）
.\package-sign-key-for-agc.ps1 -KeystorePath "sign-keystore.jks" -Alias "sign"

# 3. 上传 sign.zip 到 AppGallery Connect
```

